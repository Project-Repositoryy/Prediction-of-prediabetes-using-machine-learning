<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.healthylife">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="n">
        <activity android:name=".BmioutputActivity" />
        <activity android:name=".Output" />
        <activity android:name=".bmi" />
        <activity android:name=".machine" />
        <activity android:name=".random" />
        <activity android:name=".fasting" />
        <activity android:name=".Diabetes" />
        <activity android:name=".healthytips" />
        <activity android:name=".bloodvolume" />
        <activity android:name=".Calorielevel" />
        <activity android:name=".heartrate" />
        <activity android:name=".Menu" />
        <activity android:name=".registration" />
        <activity android:name=".MainActivity" />
        <activity android:name=".login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>